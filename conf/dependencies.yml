# Application dependencies

require:
    - play -> play [1.2.5,)
    - play -> secure
    - play -> crud
    - play -> messages 1.3
    - play -> table 1.2.2
    - commons-net 2.2

    #- play -> cobertura 2.4
    # please activate the module when it is needed,
    # it throws an error during the template rendering of watch.html
    #
    # Caused by: java.lang.VerifyError: (class:
    # controllers/ChannelSocket, method: stream signature: (I)V)
    # Expecting to find array of objects or arrays on stack
